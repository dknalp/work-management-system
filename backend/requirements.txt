fastapi>=0.115.0
pydantic[email]>=2.9.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0
httpx>=0.27.0
boto3>=1.34.0
google-auth>=2.28.0
firebase-admin>=6.5.0,<8.0.0
# google-cloud-firestore 2.20+ has a bug that URL-encodes the Firestore
# database id (default) causing a 400 InvalidArgument on every query.
# Pin to 2.19.0 which is confirmed working.
google-cloud-firestore==2.19.0
